{
   "build": {
     "builder": "DOCKERFILE",
     "dockerfilepath": "backend/Dockerfile"
},
"deploy": {
   "startCommand": "cd backend && python manage.py migrate && gunicorn edusphere.wsgi:application --bind 0.0.0.0:$PORT"
 }
}
